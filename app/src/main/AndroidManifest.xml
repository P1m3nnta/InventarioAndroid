<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inventarioweb.empresa.inventarioandroid" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".View.splashscreen"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".View.app"
            android:label=" "
            android:theme="@style/AppTheme.bar" >
        </activity>
        <activity
            android:name=".View.login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.RealizarConteo" >
        </activity>
        <activity android:name=".View.baraction.ListaArticulos" >
        </activity>
        <activity android:name=".View.baraction.ListaUbicaciones" >
        </activity>
    </application>

</manifest>
